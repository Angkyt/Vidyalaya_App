name: vidyalaya_app
description: Smart school management mobile app — student planner
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  crypto: ^3.0.3
  intl: ^0.19.0
  table_calendar: ^3.1.2
  uuid: ^4.4.0
  image_picker: ^1.0.7
  path_provider: ^2.1.3

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
